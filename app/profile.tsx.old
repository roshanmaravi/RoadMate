import { useAuth } from '@/contexts/AuthContext';
import { Ionicons } from '@expo/vector-icons';
import { LinearGradient } from 'expo-linear-gradient';
import { useRouter } from 'expo-router';
import React from 'react';
import {
  Alert,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  TouchableOpacity,
  View,
} from 'react-native';

export default function ProfileScreen() {
  const router = useRouter();
  const { userData, logout } = useAuth();

  const handleLogout = () => {
    Alert.alert(
      'Logout',
      'Are you sure you want to logout?',
      [
        { text: 'Cancel', style: 'cancel' },
        {
          text: 'Logout',
          style: 'destructive',
          onPress: async () => {
            await logout();
            router.replace('/login');
          },
        },
      ]
    );
  };

  return (
    <View style={styles.container}>
      <StatusBar backgroundColor="#667EEA" barStyle="light-content" />
      
      {/* Header with Gradient */}
      <LinearGradient
        colors={['#667EEA', '#764BA2']}
        style={styles.header}
      >
        <View style={styles.headerContent}>
          <Text style={styles.headerTitle}>My Profile</Text>
          
          {/* Profile Avatar */}
          <View style={styles.avatarContainer}>
            <LinearGradient
              colors={['#FFFFFF', '#F0F0F0']}
              style={styles.avatar}
            >
              <LinearGradient
                colors={['#4ECDC4', '#44A08D']}
                style={styles.avatarInner}
              >
                <Text style={styles.avatarText}>
                  {userData?.name ? userData.name.charAt(0).toUpperCase() : 'R'}
                </Text>
              </LinearGradient>
            </LinearGradient>
            
            <Text style={styles.userName}>{userData?.name || 'User'}</Text>
            
            <View style={styles.userInfoChips}>
              <View style={styles.infoChip}>
                <Ionicons name="call" size={12} color="#667EEA" />
                <Text style={styles.infoChipText}>{userData?.phone || 'Not set'}</Text>
              </View>
              <View style={styles.infoChip}>
                <Ionicons name="location" size={12} color="#667EEA" />
                <Text style={styles.infoChipText}>{userData?.location || 'Not set'}</Text>
              </View>
            </View>
          </View>
        </View>
      </LinearGradient>

      <ScrollView style={styles.content} showsVerticalScrollIndicator={false}>
        {/* Quick Stats */}
        <View style={styles.statsContainer}>
          <View style={styles.statBox}>
            <LinearGradient
              colors={['#F3E5F5', '#E1BEE7']}
              style={styles.statGradient}
            >
              <Ionicons name="car" size={24} color="#9C27B0" />
              <Text style={styles.statLabel}>Posted</Text>
            </LinearGradient>
          </View>
          
          <View style={styles.statBox}>
            <LinearGradient
              colors={['#FFE5E5', '#FFCCCC']}
              style={styles.statGradient}
            >
              <Ionicons name="bookmarks" size={24} color="#FF6B6B" />
              <Text style={styles.statLabel}>Booked</Text>
            </LinearGradient>
          </View>
          
          <View style={styles.statBox}>
            <LinearGradient
              colors={['#E8F5E9', '#C8E6C9']}
              style={styles.statGradient}
            >
              <Ionicons name="star" size={24} color="#4CAF50" />
              <Text style={styles.statLabel}>Rating</Text>
            </LinearGradient>
          </View>
        </View>

        {/* Account Details Card */}
        <View style={styles.card}>
          <View style={styles.cardHeader}>
            <Ionicons name="person-circle" size={24} color="#667EEA" />
            <Text style={styles.cardTitle}>Account Details</Text>
          </View>
          
          <View style={styles.detailRow}>
            <View style={styles.detailLeft}>
              <Ionicons name="person-outline" size={18} color="#667EEA" />
              <Text style={styles.detailLabel}>Full Name</Text>
            </View>
            <Text style={styles.detailValue}>{userData?.name || 'N/A'}</Text>
          </View>
          
          <View style={styles.detailRow}>
            <View style={styles.detailLeft}>
              <Ionicons name="call-outline" size={18} color="#667EEA" />
              <Text style={styles.detailLabel}>Phone</Text>
            </View>
            <Text style={styles.detailValue}>{userData?.phone || 'N/A'}</Text>
          </View>
          
          <View style={styles.detailRow}>
            <View style={styles.detailLeft}>
              <Ionicons name="location-outline" size={18} color="#667EEA" />
              <Text style={styles.detailLabel}>Location</Text>
            </View>
            <Text style={styles.detailValue}>{userData?.location || 'N/A'}</Text>
          </View>
          
          <View style={[styles.detailRow, { borderBottomWidth: 0 }]}>
            <View style={styles.detailLeft}>
              <Ionicons name="finger-print-outline" size={18} color="#667EEA" />
              <Text style={styles.detailLabel}>User ID</Text>
            </View>
            <Text style={styles.detailValue}>{userData?.userId || 'N/A'}</Text>
          </View>
        </View>

        {/* My Rides */}
        <View style={styles.section}>
          <Text style={styles.sectionTitle}>My Rides</Text>
          
          <TouchableOpacity 
            style={styles.menuItem}
            onPress={() => router.push('/(tabs)/posted-rides')}
            activeOpacity={0.7}
          >
            <View style={styles.menuLeft}>
              <LinearGradient
                colors={['#F3E5F5', '#E1BEE7']}
                style={styles.iconCircle}
              >
                <Ionicons name="car" size={24} color="#9C27B0" />
              </LinearGradient>
              <View style={styles.menuTextContainer}>
                <Text style={styles.menuLabel}>Posted Rides</Text>
                <Text style={styles.menuSubtext}>Manage your ride offers</Text>
              </View>
            </View>
            <Ionicons name="chevron-forward" size={22} color="#999" />
          </TouchableOpacity>

          <TouchableOpacity 
            style={styles.menuItem}
            onPress={() => router.push('/(tabs)/booked-rides')}
            activeOpacity={0.7}
          >
            <View style={styles.menuLeft}>
              <LinearGradient
                colors={['#FFE5E5', '#FFCCCC']}
                style={styles.iconCircle}
              >
                <Ionicons name="bookmarks" size={24} color="#FF6B6B" />
              </LinearGradient>
              <View style={styles.menuTextContainer}>
                <Text style={styles.menuLabel}>Booked Rides</Text>
                <Text style={styles.menuSubtext}>View your bookings</Text>
              </View>
            </View>
            <Ionicons name="chevron-forward" size={22} color="#999" />
          </TouchableOpacity>
        </View>

        {/* Help & Support */}
        <View style={styles.section}>
          <Text style={styles.sectionTitle}>Help & Supportrrrrr</Text>
          
          <TouchableOpacity 
            style={styles.menuItem}
            onPress={() => router.push('/help-support')}
            activeOpacity={0.7}
          >
            <View style={styles.menuLeft}>
              <LinearGradient
                colors={['#E8F5E9', '#C8E6C9']}
                style={styles.iconCircle}
              >
                <Ionicons name="help-circle" size={24} color="#4CAF50" />
              </LinearGradient>
              <View style={styles.menuTextContainer}>
                <Text style={styles.menuLabel}>Help & Support</Text>
                <Text style={styles.menuSubtext}>Get help, FAQs, contact us</Text>
              </View>
            </View>
            <Ionicons name="chevron-forward" size={22} color="#999" />
          </TouchableOpacity>

          <TouchableOpacity 
            style={styles.menuItem}
            onPress={() => router.push('/privacy-policy')}
            activeOpacity={0.7}
          >
            <View style={styles.menuLeft}>
              <LinearGradient
                colors={['#E3F2FD', '#BBDEFB']}
                style={styles.iconCircle}
              >
                <Ionicons name="shield-checkmark" size={24} color="#2196F3" />
              </LinearGradient>
              <View style={styles.menuTextContainer}>
                <Text style={styles.menuLabel}>Privacy Policy</Text>
                <Text style={styles.menuSubtext}>Read our privacy terms</Text>
              </View>
            </View>
            <Ionicons name="chevron-forward" size={22} color="#999" />
          </TouchableOpacity>
        </View>

        {/* Settings */}
        <View style={styles.section}>
          <Text style={styles.sectionTitle}>Settings</Text>
          
          <TouchableOpacity 
            style={styles.menuItem}
            activeOpacity={0.7}
          >
            <View style={styles.menuLeft}>
              <LinearGradient
                colors={['#FFF3E0', '#FFE0B2']}
                style={styles.iconCircle}
              >
                <Ionicons name="notifications" size={24} color="#FF9800" />
              </LinearGradient>
              <View style={styles.menuTextContainer}>
                <Text style={styles.menuLabel}>Notifications</Text>
                <Text style={styles.menuSubtext}>Manage notifications</Text>
              </View>
            </View>
            <Ionicons name="chevron-forward" size={22} color="#999" />
          </TouchableOpacity>

          <TouchableOpacity 
            style={styles.menuItem}
            activeOpacity={0.7}
          >
            <View style={styles.menuLeft}>
              <LinearGradient
                colors={['#E8EAF6', '#C5CAE9']}
                style={styles.iconCircle}
              >
                <Ionicons name="language" size={24} color="#3F51B5" />
              </LinearGradient>
              <View style={styles.menuTextContainer}>
                <Text style={styles.menuLabel}>Language</Text>
                <Text style={styles.menuSubtext}>English (Default)</Text>
              </View>
            </View>
            <Ionicons name="chevron-forward" size={22} color="#999" />
          </TouchableOpacity>
        </View>

        {/* Logout Button */}
        <TouchableOpacity 
          style={styles.logoutButton} 
          onPress={handleLogout}
          activeOpacity={0.8}
        >
          <LinearGradient
            colors={['#FFE5E5', '#FFCCCC']}
            style={styles.logoutGradient}
          >
            <Ionicons name="log-out-outline" size={24} color="#FF6B6B" />
            <Text style={styles.logoutText}>Logout</Text>
          </LinearGradient>
        </TouchableOpacity>

        {/* Footer */}
        <View style={styles.footer}>
          <Text style={styles.versionText}>RoadMate v1.0.0</Text>
          <Text style={styles.footerText}>Made with ❤️ in India</Text>
        </View>

        <View style={{ height: 100 }} />
      </ScrollView>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#F5F7FA',
  },
  header: {
    paddingTop: StatusBar.currentHeight || 40,
    paddingBottom: 40,
    borderBottomLeftRadius: 35,
    borderBottomRightRadius: 35,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 6 },
    shadowOpacity: 0.25,
    shadowRadius: 12,
    elevation: 10,
  },
  headerContent: {
    paddingHorizontal: 20,
  },
  headerTitle: {
    fontSize: 28,
    fontWeight: '800',
    color: '#FFFFFF',
    marginBottom: 24,
    letterSpacing: 0.5,
  },
  avatarContainer: {
    alignItems: 'center',
  },
  avatar: {
    width: 88,
    height: 88,
    borderRadius: 44,
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 12,
    padding: 4,
  },
  avatarInner: {
    width: 80,
    height: 80,
    borderRadius: 40,
    justifyContent: 'center',
    alignItems: 'center',
  },
  avatarText: {
    fontSize: 36,
    fontWeight: '800',
    color: '#FFFFFF',
  },
  userName: {
    fontSize: 24,
    fontWeight: '700',
    color: '#FFFFFF',
    marginBottom: 12,
  },
  userInfoChips: {
    flexDirection: 'row',
    gap: 12,
  },
  infoChip: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: 'rgba(255, 255, 255, 0.25)',
    paddingHorizontal: 12,
    paddingVertical: 6,
    borderRadius: 16,
    gap: 4,
  },
  infoChipText: {
    fontSize: 12,
    color: '#FFFFFF',
    fontWeight: '600',
  },
  content: {
    flex: 1,
    marginTop: -15,
  },
  statsContainer: {
    flexDirection: 'row',
    marginHorizontal: 20,
    marginTop: 20,
    marginBottom: 12,
    gap: 12,
  },
  statBox: {
    flex: 1,
    borderRadius: 16,
    overflow: 'hidden',
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.1,
    shadowRadius: 6,
    elevation: 3,
  },
  statGradient: {
    alignItems: 'center',
    paddingVertical: 16,
    gap: 4,
  },
  statLabel: {
    fontSize: 12,
    fontWeight: '600',
    color: '#666',
  },
  card: {
    backgroundColor: '#FFFFFF',
    marginHorizontal: 20,
    marginTop: 12,
    borderRadius: 20,
    padding: 20,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 3 },
    shadowOpacity: 0.12,
    shadowRadius: 10,
    elevation: 4,
  },
  cardHeader: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 10,
    marginBottom: 16,
    paddingBottom: 12,
    borderBottomWidth: 1,
    borderBottomColor: '#F3F4F6',
  },
  cardTitle: {
    fontSize: 18,
    fontWeight: '700',
    color: '#1A1A1A',
  },
  detailRow: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    paddingVertical: 14,
    borderBottomWidth: 1,
    borderBottomColor: '#F9FAFB',
  },
  detailLeft: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 10,
  },
  detailLabel: {
    fontSize: 14,
    color: '#666',
    fontWeight: '500',
  },
  detailValue: {
    fontSize: 14,
    fontWeight: '700',
    color: '#1A1A1A',
  },
  section: {
    paddingHorizontal: 20,
    marginTop: 24,
  },
  sectionTitle: {
    fontSize: 18,
    fontWeight: '700',
    color: '#1A1A1A',
    marginBottom: 12,
    paddingLeft: 4,
  },
  menuItem: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: '#FFFFFF',
    padding: 16,
    borderRadius: 18,
    marginBottom: 12,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.08,
    shadowRadius: 6,
    elevation: 2,
  },
  menuLeft: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 14,
    flex: 1,
  },
  iconCircle: {
    width: 52,
    height: 52,
    borderRadius: 26,
    justifyContent: 'center',
    alignItems: 'center',
  },
  menuTextContainer: {
    flex: 1,
  },
  menuLabel: {
    fontSize: 16,
    fontWeight: '700',
    color: '#1A1A1A',
    marginBottom: 3,
  },
  menuSubtext: {
    fontSize: 13,
    color: '#999',
    fontWeight: '500',
  },
  logoutButton: {
    marginHorizontal: 20,
    marginTop: 32,
    borderRadius: 18,
    overflow: 'hidden',
    shadowColor: '#FF6B6B',
    shadowOffset: { width: 0, height: 3 },
    shadowOpacity: 0.25,
    shadowRadius: 8,
    elevation: 4,
  },
  logoutGradient: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    paddingVertical: 18,
    gap: 10,
  },
  logoutText: {
    fontSize: 17,
    fontWeight: '700',
    color: '#FF6B6B',
  },
  footer: {
    alignItems: 'center',
    marginTop: 32,
    gap: 4,
  },
  versionText: {
    fontSize: 12,
    color: '#999',
    fontWeight: '600',
  },
  footerText: {
    fontSize: 13,
    color: '#667EEA',
    fontWeight: '600',
    fontStyle: 'italic',
  },
});
